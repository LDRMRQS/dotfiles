-- init.lua — entrypoint padrão da LazyVim (doc 03/07). Não deveria precisar de mais nada
-- aqui: toda a config real fica em lua/config/ e lua/plugins/, carregada pelo bootstrap.
require("config.lazy")
