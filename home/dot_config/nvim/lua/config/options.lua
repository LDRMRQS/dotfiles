-- options.lua — carregado automaticamente ANTES do lazy.nvim subir (doc 03).
-- Defaults completos da LazyVim: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Aqui só o que é específico do nosso setup.
vim.g.mapleader = " "
vim.g.maplocalleader = "\\"

-- kb_layout do Hyprland já é "us,br" com toggle (doc 07/hyprland.conf.tmpl) — nada a fazer
-- aqui por causa disso, é resolvido no nível do compositor, não do Neovim.
